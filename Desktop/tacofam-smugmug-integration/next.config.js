/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'photos.smugmug.com',
      'smugmug.com',
      'photos.smugmug.com',
    ],
  },
}

module.exports = nextConfig
